[build]
builder = "DOCKERFILE"

[deploy]
# Use Dockerfile CMD (shell expands $PORT). Do not override with bare $PORT here.
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
